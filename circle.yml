machine:
  timezone:
    Asia/Tokyo
  node:
    version: 0.10.33
dependencies:
  override:
    - npm install -g npm-install-missing
    - npm run vscode:prepublish
test:
  override:
    - npm test --silent
